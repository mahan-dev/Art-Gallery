import React from 'react';

const ComponentNotFound = () => {
    return (
        <div>
     
            <section>we didn't found what you've searching for ......</section>
        </div>
    );
};

export default ComponentNotFound;